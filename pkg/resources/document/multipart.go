package document

// multipart.go — CLI delegates to SDK for multipart parsing.
// ParseMultipartDocument and CreateUpdateRequest are re-exported as package-level
// vars in document.go via type aliases.
